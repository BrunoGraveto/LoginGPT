package main;

public class Utility {
    
    // Verifica o Centro da Tela para adicionar o Componente
    public static int getCenter(int screenSize, int stringSize) {
        return (screenSize - stringSize) / 2;
    }

}
